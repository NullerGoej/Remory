import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.remory',
  appName: 'remory',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
